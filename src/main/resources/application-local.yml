
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/culinaryCode
    username: developer
    password: postgresDev
    driver: org.postgresql.Driver
  flyway:
    locations: classpath:db/migration

app:
  frontend-url: http://localhost:3000
  cors:
    allowed-origins: http://localhost:3000
