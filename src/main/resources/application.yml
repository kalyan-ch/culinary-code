
spring:
  application:
    name: culinaryCode
  jpa:
    show-sql: false
    hibernate:
      ddl-auto: update
  datasource:
    url: jdbc:postgresql://localhost:5432/culinaryCode?currentSchema=recipes
    username: developer
    password: postgresDev
    driver: org.postgresql.Driver

server:
  port: 8090